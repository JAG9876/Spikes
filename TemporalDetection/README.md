# Running on linux
```
python3 -m venv .venv
source .venv/bin/activate
pip install pytest scipy matplotlib numpy
```

Then, in VSCode: select the Python Environment (bottom-right button) to be the same .venv folder

You can then click the "Testing" tab on the left sidebar to run the pytest tests
